[tools]
lefthook = "2"
node = "24"
pnpm = "12"

[tasks.setup]
description = "Install dependencies and set up git hooks"
run = ["pnpm install && pnpm exec playwright install chromium --with-deps", "lefthook install"]
